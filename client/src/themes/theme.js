import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    nature: {
      main: "#BFF6C3",
      light: "#E0FBE2",
      dark: "#dce6dc",
      contrastText: "#ACE1AF",
    },
  },
});


export default theme;